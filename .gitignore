# LaTeX intermediates (regenerable from .tex sources)
*.aux
*.log
*.out
*.toc
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.nav
*.snm
*.vrb

# (manuscript PDFs and figure PDFs are tracked)

# PNG previews are redundant with PDFs
*.png

# Python bytecode
__pycache__/
*.pyc

# Submission artefacts (kept local, not version-controlled)
cover_letter_*.txt
arxiv_metadata.txt
